# ~/.bash_main

# Custom Aliases
alias gs='git status'
alias ll='ls -lah'

# Environment Variables
export KUBUVERSE_ENV=production
export PATH="$HOME/.local/bin:$PATH"

# Load project-specific tools
source ~/projects/kubuverse/env.sh
